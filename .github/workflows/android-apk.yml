name: Build Android APK (legado - desativado)

# Mantido temporariamente apenas para histórico.
# O pipeline oficial está em .github/workflows/build-android-apk.yml.
# Não possui gatilhos automáticos para evitar builds duplicadas.
on:
  workflow_dispatch:

jobs:
  aviso:
    runs-on: ubuntu-latest
    steps:
      - name: Pipeline legado
        run: |
          echo "Workflow legado. Use build-android-apk.yml para gerar o APK oficial."
